# USB captures

Place Windows USBPcap `.pcapng` files here. Suggested names and the controlled
capture procedure are documented in `docs/WINDOWS_CAPTURE.md`.

Capture files are ignored by Git by default because they can be large and may
contain unrelated traffic from other devices on the same USB root hub.
