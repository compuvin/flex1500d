#!/bin/sh
# SPDX-License-Identifier: GPL-3.0-only
set -e

if [ "$1" = "remove" ] && command -v systemctl >/dev/null 2>&1; then
    systemctl disable --now flex1500d.service >/dev/null 2>&1 || true
fi

exit 0
